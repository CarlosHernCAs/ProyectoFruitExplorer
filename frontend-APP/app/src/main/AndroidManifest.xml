<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.permission.CAMERA" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fruits"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Actividad de Splash (la nueva primera en lanzarse) -->
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Otras actividades existentes -->
        <activity android:name=".activities.LoginActivity" android:exported="false" />
        <activity android:name=".activities.RegisterActivity" android:exported="false" />
        <activity android:name=".activities.WelcomeActivity" android:exported="false" />
        <activity android:name=".activities.ExploreActivity" android:exported="false" />
        <activity android:name=".activities.CameraActivity" />
        <activity android:name=".activities.RegionFruitsActivity" android:parentActivityName=".activities.ExploreActivity" />

        <!-- ¡NUEVA DECLARACIÓN! Aquí registramos la FruitDetailActivity -->
        <activity
            android:name=".activities.FruitDetailActivity"
            android:parentActivityName=".activities.ExploreActivity"
            android:exported="false" />

        <!-- ¡NUEVA DECLARACIÓN! Aquí registramos la RegionsListActivity -->
        <activity
            android:name=".activities.RegionsListActivity"
            android:parentActivityName=".activities.ExploreActivity" />

    </application>

</manifest>